{
  "name": "twilio-openai-realtime-call",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "env-to-yaml": "bun run scripts/env-to-yaml.ts .env",
    "build": "bun build src/index.ts --target bun --outdir build --minify",
    "deploy": "./scripts/deploy.sh"
  },
  "dependencies": {
    "hono": "^4.6.16",
    "twilio": "^5.4.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
